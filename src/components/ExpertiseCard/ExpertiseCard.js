import React from 'react';
import styles from './ExpertiseCard.module.css';

const ExpertiseCard = () => (
  <div className={styles.ExpertiseCard}>
    <p>Une expertise totale, une présence locale</p>
  </div>
);

export default ExpertiseCard;
